// Empty stub for Node.js built-in modules (node:fs, node:path, etc.).
// These modules are only used by SDK code paths that run in Node.js environments
// and are never reached in React Native — this stub prevents Metro from erroring.
module.exports = {};
